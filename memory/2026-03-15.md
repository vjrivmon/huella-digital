# 2026-03-15 — Sesión Fallas Valencia / Encesa

## Contexto
Día de Fallas. Vicente en Valencia capturando fallas con la app Encesa.
Sesión intensa de bug fixing y nuevas features en tiempo real.

## App Encesa
- **Live:** https://vjrivmon.github.io/encesa/
- **Repo:** github.com/vjrivmon/encesa (gh-pages para deploy)
- **Supabase:** zvljpppcdbawicamhbix.supabase.co
- **Deploy:** manual con `cp -r app/dist/* . && git push origin gh-pages --force`

## Bugs resueltos hoy

### 1. Sync mostraba "Sin sincronizaciones previas" en PWA vs web
- Causa: `localStorage` aislado entre PWA y browser en iOS
- Fix: comportamiento esperado, explicado a Vicente

### 2. 702 fallas fallaban en sync
- Causa: `fetch('data:...')` no funciona en Safari iOS para imágenes grandes
- Fix: reemplazado por `dataUrlToBlob()` usando `atob()` directamente

### 3. Completitud al 95% aunque checklist completo
- Causa: bonus de fotos (5% por 3+, 5% por 6+) imposibilitaba 100% con 1 foto
- Fix: eliminados bonuses, fórmula limpia: OCR(30) + foto(35) + valoración(25) + notas(10) = 100%

### 4. Galería mostraba porcentaje desactualizado (95% en vez de 100%)
- Causa: leía `completitud_pct` del DB (valor viejo)
- Fix: GalleryView recalcula al vuelo con `calcularCompletitud()` y actualiza DB

### 5. Importar desde URL (Twitter) — múltiples iteraciones
- cobalt.tools v11 ahora requiere JWT → descartado
- Reemplazado por **fxtwitter API** (CORS abierto, devuelve video URLs)
- Twitter bloquea `fetch()` con Sec-Fetch-Mode:cors desde browser
- Montado **proxy HTTPS** en el servidor: `https://099ffd8b696f20ee-77-42-16-230.serveousercontent.com`
  - Puerto: 3099 en localhost, tunnelado con serveo.net
  - Solo permite `video.twimg.com`
  - Mantener activo: `nohup node /tmp/video-proxy.mjs > /tmp/proxy.log 2>&1 &`
  - Tunnel: `nohup ssh -o StrictHostKeyChecking=no -o ServerAliveInterval=60 -R 80:localhost:3099 serveo.net > /tmp/serveo.log 2>&1 &`
  - ⚠️ URL cambia si se reinicia el tunnel

## Features implementadas hoy

### Sync bidireccional (pull desde Supabase)
- `pullFromSupabase.ts` — descarga fallas, fotos, valoraciones de Supabase → IndexedDB
- Auto-pull al arrancar la app (App.tsx useEffect)
- Botón manual "Actualizar desde nube" (azul) en SyncView

### Grid de fotos + Lightbox en FallaSheet
- Grid 3×N entre boceto y checklist
- Punto naranja en fotos no sincronizadas
- Tap → lightbox fullscreen con flechas prev/next + strip thumbnails

## Estado fotos en Supabase a las 13:08
- falla-0332 (Convent de Jerusalem-Matemàtic Marzal, Especial): 7 fotos
- falla-0676 (Espartero-Ramón y Cajal Infantil, 3a): 8 fotos
- Total: 16 fotos + 1 test

## Notas técnicas
- iOS PWA y browser Safari tienen IndexedDB separados (explicado a Vicente)
- Service worker muy testarudo en iOS → hay que eliminar PWA y re-añadirla
- `git checkout gh-pages` para deploy: copiar dist/* a la raíz del branch

## Proxy server (temporal para Fallas 2026)
- Script: `/tmp/video-proxy.mjs`
- Log: `/tmp/proxy.log`
- Log serveo: `/tmp/serveo.log`
- IP servidor: 77.42.16.230 / 2a01:4f9:c013:33c3::1

---

## Sesión tarde/noche (16:00-21:00 UTC)

### Features añadidas
- **TikTok en UrlImporter**: tikwm API → URL video → proxy → ffmpeg frames server-side
- **Extracción frames server-side** (ffmpeg): elimina todos los problemas de iOS canvas/crossOrigin
- **Filtros Captura**: Pendientes / Favoritas (default: Pendientes) + fila de categoría
- **Paginación FallasList**: 50 cards por carga + "Cargar más" (fix OOM)
- **Grid fotos + Lightbox** en FallaSheet
- **Sync bidireccional**: pullFromSupabase + botón "Actualizar desde nube" en SyncView
- **Widget "Progreso del modelo"** en SyncView: 176 fotos servidor, hitos 300/700/2000
- **Sync sube valoraciones** a Supabase (antes solo subía fotos)

### Bugs críticos resueltos
1. **OOM crash iOS** ("problemas repetidamente"): 3 causas encontradas y eliminadas
   - GalleryView renderizaba 703 fallas con boceto images → filtrado a solo fallas con fotos
   - pullFromSupabase() auto en startup → eliminado (ahora solo manual)
   - FallasList renderizaba ~683 cards con SVGs → paginación 50/vez
2. **Estado falla cambia solo al abrirla**: auto-marcado de `ocr_realizado=true` al abrir FallaSheet → eliminado
3. **Fórmula completitud**: quitado el bonus de OCR (30% automático), redistribuido: fotos(50%) + val(35%) + notas(15%)
4. **Valoraciones no se sincronizaban**: sync ahora también sube valoraciones a Supabase
5. **Falla "Test" eliminada** de Supabase manualmente

### Infraestructura tunnel/proxy
- **Proxy node.js**: `/tmp/video-proxy.mjs` en puerto 3099
  - Permite: `video.twimg.com`, `*.tiktokcdn.com`, `*.tiktokcdn-us.com`
  - Modo /frames: descarga vídeo + ffmpeg → devuelve array de base64 JPEGs
- **Tunnel localtunnel**: `--subdomain encesa-fallas` → `https://encesa-fallas.loca.lt`
- **Servicios systemd**: `encesa-proxy` + `encesa-tunnel` (Restart=always)
- **URL dinámica**: `proxy-config.json` en gh-pages (sin rebuild al cambiar URL)
- **⚠️ loca.lt cierra conexiones cada ~15 min** → systemd reinicia automáticamente

### Estado Supabase al final del día
- 176 fotos de 20 fallas distintas (media 8.8 fotos/falla)
- 2 valoraciones subidas
- Fórmula actual: fotos(50%) + valoración(35%) + notas(15%)

### Para reinstalar PWA tras crash iOS
1. Eliminar app del home screen
2. Ajustes → Safari → Avanzado → Datos de sitios web → borrar vjrivmon
3. Safari → vjrivmon.github.io/encesa/ → Compartir → Añadir a pantalla de inicio

## LinkedIn post (pendiente publicar tras Fallas 19 marzo)
- Borrador guardado en esta sesión
- Publicar cuando acaben las fallas con datos finales (fotos totales, fallas cubiertas)
- Tono: personal, auténtico, combina pasión fallas + IA
- Añadir métricas reales y resultado de entrenamiento si hay tiempo
- Hashtags: #Fallas2026 #GenerativeAI #StableDiffusion #Valencia #AIArt

## Proyecto IA Fallas — decisiones técnicas
- Objetivo: herramienta generativa para artistas falleros en busca de inspiración
- Approach: LoRA sobre SDXL (mejor balance calidad/entrenabilidad con RTX 4070 12GB)
- Hardware entrenamiento: Slimbook en casa (RTX 4070)
- Tiempo estimado: 4-6h entrenamiento SDXL LoRA
- Output: 1024x1024 concept art de fallas nuevas a partir de prompt de texto
- Dataset: fotos Supabase + lema/artista/categoría como captions
- Estado: pendiente pipeline exportación + entrenamiento (cuando acaben fallas)
- FLUX.1 descartado por ahora — 16-24GB VRAM, 4070 tiene 12GB
