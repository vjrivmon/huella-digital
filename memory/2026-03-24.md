# 2026-03-24 — Sesión intensa: Catan Coach

## Benchmark
- Score real: **~85%** (17/20) — el 20/20 era falso positivo
- C3: producción evasiva, C4: 0 PV con 2 poblados, C5: confusión ciudad vs carta
- Fix aplicado: system prompt con PV pre-computados, tabla producción por dado, 5 reglas obligatorias

## Bugs corregidos
- **DevCardStepper en turno 0** — saltar directo al dado al iniciar partida
- **"El Colonizador"** — LLM usaba nombre antiguo, fix en ambos system prompts
- **coach-recommend crash** — settlements/cities/roads llegaban undefined

## Features implementadas

### ActionMenu (móvil + desktop)
- Móvil: botón `+` izquierda input → popup acciones contextuales
- Desktop: chips encima del input siempre visibles
- Acciones: actualizar recursos, cartas dev, mover ladrón, actualizar tablero, siguiente turno

### Fase A — GeneticAgent con rivales
- `other_players[]` construido desde savedPieces + savedAssignments
- `knights_played`, `longest_road`, `largest_army` ahora se pasan
- Activa: knight_take_largest_army_bonus(1.40), knight_for_win_bonus(7.18), rival awareness

### Fase B — Topología real de vértices
- `buildTopology()` en route: pre-computa vertex→hexIndices desde geometría BoardOverlay
- `_get_adjacent_hexes()` en Python ya no retorna [] — usa adjacent_hexes reales
- `multiple_high_prob_numbers_bonus` (4.665) ahora se aplica en ciudades
- `describeVertex()` / `describeEdge()` — descripciones humanas de posición
- `positionContext` en respuesta: frontier (vértices de expansión con terrenos)

### System prompt coach reestructurado
- `computeVP()`: PV pre-calculados, regla "no digas 0 si hay poblados"
- `computeProductionTable()`: dado→recurso para piezas del jugador
- Sección REGLAS DE RESPUESTA OBLIGATORIAS (5 reglas)
- `positionBlock`: vértices de expansión con descripción de terrenos

### Fases 1-4 — LLM → acción visual en tablero
- **Fase 1**: LLM emite `RECOMMENDATION_JSON:{type,position,label}` al final del stream
  - `extractRecommendation()` parsea y limpia el texto visible
  - route/chat extrae y envía en evento `done` como `boardRecommendation`
- **Fase 2**: MessageBubble muestra botón "Ver [pieza] en tablero →" si hay recomendación
- **Fase 3**: BoardOverlay con `previewRecommendation` — aura SVG pulsante (CSS keyframes)
  - Vértice: 3 círculos concéntricos animados
  - Arista: glow line + línea sólida + flecha direccional
  - Bottom bar alternativo con "Confirmar jugada →"
- **Fase 4**: `onConfirmRecommendation` registra pieza en savedPieces automáticamente

### Fix contexto en preguntas manuales
- Si pregunta estratégica sin recursos → pedir recursos primero (no responder vacío)
- Si hay recursos pero no GeneticRec → consultar GeneticAgent on-demand antes del LLM

### start-all.sh
- `git pull --rebase --autostash` en ambos repos al inicio
- Mata puertos :8001 y :3000 (NO :8000 — ChromaDB está en Docker)
- Detecta ChromaDB con `curl heartbeat`, hace `docker start catan-chroma` si existe
- ChromaDB en Docker: contenedor `catan-chroma` en puerto 8000

### Repo catan-advisor-api
- `__pycache__` eliminados del tracking (estaban en .gitignore pero rastreados)
- Ahora `git pull` no da conflictos

## Estado repos
- `catan-coach`: branch `master`, último commit `aaf9a5e`
- `catan-advisor-api`: branch `main`, último commit `55997f7`

## Pendiente
- Benchmark nuevo tras fixes del system prompt (relanzar en Slimbook)
- Fase C: puertos en BoardOverlay (impacto bajo-medio)
- Tutorial/onboarding actualizado con el nuevo flujo
- Ladrón movible desde el tablero
