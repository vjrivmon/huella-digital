# 2026-03-19 — Catan Coach Fase 2 + Pairing Slimbook

## Pairing Slimbook
- Vicente quiere conectar su Slimbook (RTX 4070) como nodo al gateway
- Error: gateway solo escuchaba en loopback → fix: `gateway.bind=lan` + `publicUrl=ws://77.42.16.230:18789`
- Plugin `device-pair` habilitado en config
- Próximo paso: Vicente envía `/pair` para obtener setup code y conectar el Slimbook

## Catan Coach — Fase 2 Frontend (catan-chatbot)

### Lo que quedamos (D1-D5)
- D1: Dos modos separados desde landing: "Aprende Catán" y "Coach en partida"
- D2: GameState en servidor (Redis/JSON TTL 6h) + código tablero
- D3: Stepper de recursos obligatorio, arranca en 0, sin saltarse
- D4: Sin SVG, confirmación textual del análisis
- D5: ML con best_chromosome.json (93 parámetros del GeneticAgent)

### Implementado hoy
- `/` → Landing con dos tarjetas de modo
- `/aprende` → ChatInterface original intacto, con botón ← volver
- `/coach` → Flujo 3 pasos: upload foto → stepper recursos → recomendación
- Componentes creados: AdvisorMode, BoardUpload, ResourceStepper, CoachResult
- Commit: fb3a79c en master de catan-chatbot

### Primera versión rechazada por Vicente
- Toggle en header era incorrecto → lo correcto es landing con rutas separadas

### Estado final
- Build limpio (0 errores TypeScript)
- Servidor dev en puerto 3200
- Evidencias (screenshots) enviadas a Vicente por Telegram

## Catan Coach — Diseño UI (tarde 19 marzo)

### Visión final acordada (prototipo v3)
- SIN landing de dos modos — una sola pantalla de chat Coach
- Icono hexágono SVG en top-right (trigger del coach)
- Al pulsarlo: bottom sheet con DOS tarjetas GRANDES: Foto / Tablero interactivo
- Cámara con overlay de encuadre (cuadrícula + esquinas doradas)
- Tablero interactivo: CSS + texturas REALES del PyCatan Visualizer (quarry, forest, cereal, wool, mountain, desert)
- Piezas: nodos/roads del Visualizer, interactivos (click en vértice → pueblo/ciudad, arista → camino)
- Números de game_0.json sobre círculos blancos
- Stepper de recursos + recomendación: dentro del chat, misma estética
- Estado tablero persiste en sesión (no se pierde al cerrar overlay)
- Prototipo: `catan-coach-ux/prototype-v3.html`

### ⚠️ ARQUITECTURA FINAL (tras screenshot de Vicente — 15:37)
**EL CHATBOT ES LA PANTALLA DE INICIO. No hay landing. No hay rutas separadas.**

- `app/page.tsx` → ChatInterface (chatbot Aprende) — es el home
- Añadir icono hexágono SVG en top-right del header existente
- Pulsar hexágono → pantalla grande con 2 opciones: Foto / Tablero interactivo
- Cámara: overlay con guía de encuadre
- Tablero: PyCatan assets, `--s` reducida (~52px) para móvil, sin transform scale
- Resultados (stepper recursos + recomendación) vuelven al chat
- Para volver al chat normal: pendiente confirmar si toggle o botón ←
- NO rutas /aprende ni /coach. TODO en un sitio.

### Estado actual (17:44 UTC)

**Commits del día:**
- `aa3a8ed` — roads onClick + debounce (fix double-fire iOS)
- `45b933b` — puertos 2:1 y 3:1 posiciones estándar
- `e85c8a2` — puertos como pills simples (icono + ratio)
- `fa97615` — ⬡ siempre visible, tablero persiste y reabre directo

**Arquitectura coach mode (decisiones firmadas):**
- ⬡ siempre visible en header (no se reemplaza por 💬)
- Dot ámbar = sin tablero; dot verde = tablero activo en sesión
- 1er tap ⬡ → modal 2 opciones (Foto / Tablero)
- Tap ⬡ con tablero activo → abre tablero con piezas guardadas (actualización mid-game)
- "Nueva conversación" → resetea todo (coach state + savedPieces + chat)
- NO hay botón "Nueva partida" — redundante con "Nueva conversación"

**Key state:**
- `savedPieces: Record<string, Piece>` en ChatInterface persiste entre aperturas del tablero
- `boardConfigured = Object.keys(savedPieces).length > 0` controla comportamiento del ⬡
- `initialPieces` prop en BoardOverlay para restaurar estado al reabrir

**Pendiente:**
- Integración backend: `POST /coach/recommend` con board + resources reales
- Test en dispositivo real de Vicente (caminos en aristas — fix onClick pendiente de confirmar)
- Verificar que la URL del tunnel sigue activa (https://institution-declined-only-civilization.trycloudflare.com)

## Fallas 2026 — Post Fallas
- Hoy 19 marzo es el último día de Fallas
- Pipeline LoRA pendiente ejecutar (post-fallas)
- LinkedIn post pendiente publicar con datos finales

## LinkedIn Post
- Borrador guardado de sesión 15 marzo
- Publicar cuando acaben las Fallas con métricas reales de Encesa
